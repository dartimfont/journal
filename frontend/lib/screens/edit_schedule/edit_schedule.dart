import 'package:flutter/material.dart';

import 'component/body.dart';

class EditScheduleScreen extends StatelessWidget {
  static String routeName = "/edit_schedule";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body()
    );
  }
}
